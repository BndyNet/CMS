namespace CMS.Models
{
    using System;
    
    public enum PageType : int
    {
        Url = 10,
        SinglePage = 20,
        ArticleList = 30,
        ProductList = 40,
        DownloadList = 50,
        None = 0
    }
}
